# `kesselrun` Example

To run the examples:

1. Install dependencies:

    ```sh
    $ npm install
    ```

2. Execute the NPM script:

    ```sh
    $ npm run bench
    ```
Results will display in the terminal.
